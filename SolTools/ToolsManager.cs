namespace SolTools.Managers;

using Godot;
using fennecs;
using SolTools.Components;

public static class ToolsManager
{
	public static bool Equip(Entity entity)
    {
        return true;        
    }

    public static bool Unequip(Entity entity)
    {
        return true;
    }
}